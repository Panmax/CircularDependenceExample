消除循环依赖的代码示例

```
.
├── README.md
└── src
    └── com
        └── jpanj
            └── depend
                ├── callback  // 采用回调方式
                ├── circle  // 不采取任何方式
                ├── mediator // 提取中介者
                └── transfer  // 转移业务逻辑

```